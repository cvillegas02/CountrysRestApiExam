<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppCountrys.Views.NewItemPage"
             Title="New Item">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Alpha" FontSize="Medium" />
            <Entry Text="{Binding Alpha}" FontSize="Small" />
            <Label Text="Name" FontSize="Medium" />
            <Entry Text="{Binding Name}" FontSize="Small" Margin="0" />
            <Label Text="Numeric Code" FontSize="Medium" />
            <Entry Text="{Binding Code}" FontSize="Small" />
            
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
